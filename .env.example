# Copy this file to .env.local and paste the SAS URL we sent you.
# .env.local is gitignored — never commit a SAS.

AZURE_SAS_URL=

# Already correct — no need to change these.
AZURE_STORAGE_CONTAINERS=division-a,division-b,division-c
SQLITE_PATH=claims.db
PORT=8080
